import 'package:flutter/material.dart';

class TabletPortraitList extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return GridView(gridDelegate: null,);
  }
}


class TabletLandscapeList extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return GridView(gridDelegate: null,);
  }
}
